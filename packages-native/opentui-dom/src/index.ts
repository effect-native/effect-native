/**
 * @effect-native/opentui-dom
 *
 * Effect-based DOM adapter interface for unified DOM access
 * across different environments (happy-dom, puppeteer, etc.).
 *
 * @since 0.1.0
 */

/**
 * Re-export of the `DOMAdapter` module.
 *
 * @since 0.1.0
 */
export * from "./DOMAdapter.js"

/**
 * Re-export of the `TestAdapter` module.
 *
 * @since 0.1.0
 */
export * from "./TestAdapter.js"

/**
 * Re-export of the `ThemeMap` module.
 *
 * @since 0.1.0
 */
export * from "./bridge/theme-map.js"

/**
 * Re-export of the `EventRelay` module.
 *
 * @since 0.1.0
 */
export * from "./bridge/event-relay.js"

/**
 * Re-export of the `NodeMap` module.
 *
 * @since 0.1.0
 */
export * from "./bridge/node-map.js"
