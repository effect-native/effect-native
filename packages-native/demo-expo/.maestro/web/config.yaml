# Maestro workspace config for demo-expo web testing
env:
  WEB_URL: http://localhost:8081  # Default Expo web port
  
tags:
  - web
  - smoke
  - navigation

# Platform-specific settings
platform: web