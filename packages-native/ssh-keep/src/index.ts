/**
 * ssh-keep: SSH+tmux resilient entrypoint with session picker
 *
 * This module exports utilities for programmatic use.
 * For CLI usage, run `ssh-keep` or `npx ssh-keep`.
 *
 * See SPEC.md for full behavioral specification.
 */

export const VERSION = "0.0.1-placeholder"
