# Gaps Snapshot: .ok/v4.ok.md

Captured: 2026-02-24 09:12:54 -0500
Branch: v4
HEAD: f806603d9
Bun: 1.3.5
Node: v24.12.0

## Gates

- Gate 1 (bun run ok): PASS (exit 0)
- Gate 2 (bun run test): PASS
- Gate 3 (workspace path is packages/): PASS
- Gate 4 (bun run check && bun run test): PASS
- Gate 5 (release channel from origin/v4 is beta): PASS
  - `.github/workflows/release.yml` runs `bun run changeset-publish-beta` on `v4`.
  - `package.json` now uses `changeset publish --tag beta` in `changeset-publish-beta`.
- Gate 6 (effect-named package versions are v4): PASS

## Non-Gated Notes

- `@effect-native/opentui-dom-testing-library` build is stubbed (echo) but exits 0, so `bun run ok` still passes.
- `packages/examples-tui-testing-library/test/lazygit.test.ts` emits `warning: unhandled Platform key FamilyDisplayName` and may skip when lazygit fails to start.
- The lazygit skip behavior is an explicit, narrow exception in `AGENTS.md` under Hard-Fail Policy.

## Incident Check (npm dist-tags)

- Verified the released v4 public packages currently have `beta=4.0.0` and stable `latest` tags.

## Context From Prior Tasks (Purged)

- npm OIDC trusted publishing previously failed with OIDC exchange 404 for: `effect-native`, `@effect-native/debug`, `@effect-native/patterns`, `@effect-native/schemas`.
- Many prior notes referenced `packages-native/`; current workspaces are `packages/*`.
